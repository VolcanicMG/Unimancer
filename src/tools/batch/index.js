/** Meta — batch execution (run several tools in one MCP call). */
import { batchExecute } from "./batchExecute.js";
/** @type {import("../../core/types.js").ToolDefinition[]} */
export const batchTools = [batchExecute];

# depguard: never auto-run lifecycle scripts; 'guard install' handles approvals.
ignore-scripts=true
# depguard: pin new deps to the exact installed version (no ^/~); bump manually.
save-exact=true
save-prefix=
